meta {
  name: api_version
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api
  body: none
  auth: none
}

tests {
  test("validate_status", function(){
    expect(res.getStatus()).to.equal(200);
  });
  
  test("validate_body", function(){
    const data = res.getBody();
    expect(data).to.equal("v0.0.2");
  });
}
